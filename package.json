{
  "name": "fullstack_app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd backend && nodemon server.js\" \"cd client && npm start\"",
    "heroku-postbuild":
      "NPM_CONFIG_PRODUCTION=false cd backend && npm install && cd ../client && npm install --prefix client && npm run build --prefix client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.0.1",
    "nodemon": "^1.19.0",
    "socket.io": "^2.2.0"
  }
}
